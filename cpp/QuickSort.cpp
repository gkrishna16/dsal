#include <iostream>
#include <vector>
using namespace std;

// choose a pivot element
// Partitioning
// all the smaller than pivot on the left
// all the greater than pivot on the right
// equal to it can go anywhere

void quickSort(int nums[])
{
    for (int i = 0; i < sizeof(nums) / sizeof(nums[0]); i++)
    {
    }
}

int main()
{
}