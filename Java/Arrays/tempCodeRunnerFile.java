 // public static boolean ValidSudoku(int nums[][]) {
    // var seen = new HashSet<String>();

    // for (int i = 0; i < 9; i++) {
    // for (int j = 0; j < 9; j++) {
    // char current_val = nums[i][j];
    // if (current_val != '.') {
    // if (!seen.add(current_val + " found int the row " + i)
    // || !seen.add(current_val + " found in the column " + j)
    // || !seen.add(current_val + " found in the sub box " + i / 3 + "-" + j / 3)) {
    // return false;

    // }

    // }

    // }

    // }

    // return true;
    // }
